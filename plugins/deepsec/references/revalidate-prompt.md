# Revalidate prompt

Re-check each finding. Verdicts: true-positive | false-positive | fixed | uncertain.
Use git history when available for "fixed". Return JSON array of { filePath, vulnSlug, title, verdict, reasoning }.
